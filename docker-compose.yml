services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
    volumes:
      - ./src:/app/src:ro
